createTable=CREATE TABLE IF NOT EXISTS users (name VARCHAR(100) NOT NULL, age INT NOT NULL)
insertUser=INSERT INTO users(name, age) VALUES (?, ?)
findAllUsers=SELECT name, age FROM users
clearUsers=DELETE FROM users